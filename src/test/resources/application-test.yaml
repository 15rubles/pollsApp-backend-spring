spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/polls_app_test'
    username: root
    password: '1111'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true